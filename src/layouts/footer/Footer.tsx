import { memo } from "react";
import "./Footer.scss";

const Footer = () => {
  return (
    <footer className="only-btc__layout__footer">
      This is Footer
    </footer>
  );
}

export default memo(Footer);
