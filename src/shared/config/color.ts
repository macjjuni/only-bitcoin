/**
 * 비트코인 브랜드 색.
 *
 * CSS · className 은 `globals.css` 의 `--bitcoin-rgb`( Tailwind `bitcoin` )를 씀.
 * JS 값이 필요한 곳( 차트 옵션 · color prop )만 이 상수를 씀.
 * 두 곳이 같은 색이라 바꿀 때 같이 바꿔야 함.
 */
export const BITCOIN_COLOR = "#F7931A";
